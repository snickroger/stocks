version: '3.8'
services:
  app:
    image: node:20.11
    working_dir: /stocks
    container_name: stocks_app
    command: sh -c "yarn start"
    ports:
      - "9229:9229"
    env_file: variables.env
    volumes:
      - .:/stocks
